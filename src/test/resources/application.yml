spring:
  profiles:
    active: test
  datasource:
    driver-class-name: org.h2.Driver
  data:
    redis:
      host: localhost
      port: 6379
host: localhost
kafka:
  host: localhost:9092
  consumer-group: feed
  topic: feed
